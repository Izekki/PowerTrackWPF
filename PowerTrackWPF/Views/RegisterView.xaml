<Window x:Class="PowerTrackWPF.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PowerTrackWPF"
        mc:Ignorable="d"
        Title="Registro" Height="600" Width="450" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Width="360">

            <!-- Título con botón regresar -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,10">
                <TextBlock Text="←" FontSize="20" Cursor="Hand" MouseDown="GoBack_Click" Margin="0,0,10,0"/>
                <TextBlock Text="Registro" FontSize="24" FontWeight="Bold"/>
            </StackPanel>

            <!-- Nombre -->
            <TextBlock Text="Introduzca su nombre:" Margin="0,20,0,5"/>
            <DockPanel>
                <Image Source="/Assets/name-icon.png" Width="20" Height="20" Margin="0,0,5,0"/>
                <TextBox x:Name="NombreTextBox" Width="300" />
            </DockPanel>

            <!-- Correo -->
            <TextBlock Text="Correo electrónico" Margin="0,20,0,5"/>
            <DockPanel>
                <Image Source="/Assets/email-icon.png" Width="20" Height="20" Margin="0,0,5,0"/>
                <TextBox x:Name="CorreoTextBox" Width="300" />
            </DockPanel>

            <!-- Contraseña -->
            <TextBlock Text="Contraseña" Margin="0,20,0,5"/>
            <DockPanel>
                <Image Source="/Assets/password-icon.png" Width="20" Height="20" Margin="0,0,5,0"/>
                <PasswordBox x:Name="PasswordBox" Width="260"/>
                <Button x:Name="TogglePasswordBtn" Width="30" Background="Transparent" BorderThickness="0" Click="TogglePassword_Click">
                    <Image x:Name="EyeIcon" Source="/Assets/eye-icon.png" Width="20"/>
                </Button>
            </DockPanel>

            <!-- Confirmar contraseña -->
            <TextBlock Text="Confirmar contraseña" Margin="0,20,0,5"/>
            <DockPanel>
                <Image Source="/Assets/password-icon.png" Width="20" Height="20" Margin="0,0,5,0"/>
                <PasswordBox x:Name="ConfirmPasswordBox" Width="260"/>
                <Button x:Name="ToggleConfirmPasswordBtn" Width="30" Background="Transparent" BorderThickness="0" Click="ToggleConfirmPassword_Click">
                    <Image x:Name="ConfirmEyeIcon" Source="/Assets/eye-icon.png" Width="20"/>
                </Button>
            </DockPanel>

            <!-- Proveedor -->
            <TextBlock Text="Proveedor" Margin="0,20,0,5"/>
            <ComboBox x:Name="ProveedorComboBox" Width="300" />

            <!-- Botón de registro -->
            <Button Content="Registrarse" Margin="0,30,0,0" Width="150" Click="RegisterButton_Click" RenderTransformOrigin="0.507,-0.884"/>

        </StackPanel>
    </Grid>
</Window>
